<%= render "shared/admin/nav", { header_title: get_header_title_cash_flow(params[:transactable_type].underscore, @cashier_shift), back_icon: true } do %>
<% end %>

<div class="container p-6 mx-auto mt-6 bg-white rounded-lg border shadow border-slate-300/80 shadow-slate-100 dark:shadow-slate-950 dark:bg-slate-800 dark:border-slate-600/80">
  <h1 class="mb-4 text-2xl font-bold">Agregar <%= t("#{params[:transactable_type].underscore}") %></h1>
  <%= render 'form' %>
</div>