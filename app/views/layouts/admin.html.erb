<!DOCTYPE html>
<html lang="en">

<head>
  <title>Admin | <%= Railsui.config.application_name %></title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  <%= stylesheet_link_tag "application", "https://unpkg.com/trix@2.0.0/dist/trix.css", "data-turbo-track": "reload" %>
  <%= action_cable_meta_tag %>
  <%= turbo_include_tags %>
  <%= turbo_refreshes_with method: :morph, scroll: :preserve %>

  <%= stylesheet_link_tag 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css' %>
  <%= stylesheet_link_tag 'https://releases.transloadit.com/uppy/v2.0.0/uppy.min.css' %>
  <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/css/tom-select.css' %>
  <%= stylesheet_link_tag 'https://cdn.datatables.net/2.1.3/css/dataTables.dataTables.min.css' %>

  <%= render "shared/fonts" %>
  <%= render "shared/meta" %>
  <%= custom_colors_css %>
  <%= yield :head %>
</head>

<body class="h-full antialiased rui-admin bg-slate-50 dark:bg-slate-900 dark:text-slate-100 text-slate-800">
  <%= render "shared/flash" %>
  <%= render "shared/loader" %>

  <%= yield %>

  <%= render "shared/admin/debug_footer" %>

  <div data-controller="custom-modal">
    <div data-custom-modal-target="container" class="fixed inset-0 z-50 flex items-center justify-center hidden overflow-y-auto bg-black/80" role="dialog">
      <div class="relative w-full max-w-2xl p-8 m-4 bg-white rounded shadow-xl dark:bg-slate-700">
        <div data-custom-modal-target="content">
          <!-- Content will be inserted here dynamically -->
        </div>
      </div>
    </div>
  </div>


  <%= railsui_launcher if Rails.env.development? %>
</body>
</html>
