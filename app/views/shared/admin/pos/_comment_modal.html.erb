<div data-pos--comment-modal-target="container" class="fixed inset-0 z-50 hidden overflow-y-auto" role="dialog">
  <div class="relative w-full h-screen">
    <div data-pos--comment-modal-target="content" class="hidden h-auto max-w-2xl p-8 m-4 mx-auto bg-white rounded-lg shadow-xl dark:bg-slate-700">
      <h2 class="text-lg font-semibold text-center dark:text-slate-100">Agregar Comentarios</h2>
      <textarea id="order-comments" class="w-full p-2 mt-4 border rounded-md dark:bg-slate-600 dark:text-white" rows="5" placeholder="Escribe tus comentarios aquí..."></textarea>
      <div class="flex justify-end mt-4 space-x-4">
        <button type="button" class="btn btn-secondary" data-action="click->pos--comment-modal#close">Cancelar</button>
        <button type="button" class="btn btn-primary" data-action="click->pos--comment-modal#saveComment">Guardar</button>
      </div>
    </div>
  </div>
</div>